import {useForm} from 'react-hook-form';
import zodResolver from '@hookform/resolvers/zod';
import ProfileFormData from '../../../lib/validation/postedit';



const ProfileForm = ({handleEditProfile}) => {
  return (
    <form>
            
    </form>
  )
}

export default ProfileForm